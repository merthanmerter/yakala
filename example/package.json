{
	"name": "yakala-test-project",
	"version": "1.0.0",
	"description": "Test project for yakala library",
	"main": "dist/index.js",
	"scripts": {
		"prebuild": "cd .. && npm run build",
		"build": "tsc",
		"start": "node dist/index.js",
		"dev": "tsc --watch",
		"test": "npm run build && npm start"
	},
	"dependencies": {
		"yakala": "latest"
	},
	"devDependencies": {
		"typescript": "^5.0.0",
		"@types/node": "^20.0.0"
	}
}
